# Tasks: Earnings Watchlist & Morning Pipeline (PRD 0008)

**PRD:** `tasks/0008-prd-earnings-watchlist-morning-pipeline.md`
**Generated:** 2026-02-26

## Relevant Files

- `strategies/earnings_intel/ei_post_earnings_calc.py` - Update IV crush severity scale from 4-tier to 5-tier at two code locations (Req 20-21)
- `strategies/earnings_intel/ei_backfill_metrics.py` - Update matching IV crush classification for consistency (lines 128-138)
- `strategies/earnings_intel/ei_watchlist.py` - **NEW FILE** — Watchlist writer module for `earnings_watchlist` table (Req 11-16, 27)
- `strategies/earnings_intel/ei_main.py` - Restructure into unified 6-sub-step `run_daily_pipeline()` (Req 7, 9-10, 28)
- `main.py` - Step renumbering: STEP_SEQUENCE (lines 154-174), step_durations keys, coffee_break calls, results dict keys, CLI flag (Req 1-6)
- `main_runners.py` - New `run_earnings_intelligence()` replacing `run_earnings_pipeline()` (line 1006) + `run_earnings_morning_scan()` (line 1100), plus console table renderer (Req 8, 17-18, 29)
- `strategies/flow_monitor/fm_watchlist.py` - Add `earnings_play_signal` + `relative_underpricing_pct` to earnings enrichment in `_fetch_earnings_lookup()` (line 33) and `update_daily_watchlist()` (line 83) (Req 22-23, 32)
- `tools/performance_writer.py` - Update `ei_mapping` (line 635), add 5 columns to `ei_pipeline_performance` DDL (line 101), ALTER TABLE migration (Req 24-26, 31)
- `tools/news_sentiment.py` - Called as pipeline sub-step 5 via `enrich_watchlist_batch()`; no changes needed, integration point only
- `tools/decimal_formatter.py` - `clean_database_row()` used by ei_watchlist.py for all numeric values
- `tools/log_utils.py` - Console output toolkit used by orchestrator runner for table display
- `docs/CONSOLE_DEVELOPER_GUIDE.md` - Reference for console output patterns

### Notes

- No formal test framework — validation is manual testing with `--simulate-time` and `--earnings-intel` CLI flags
- IV crush severity classification exists in THREE places: `ei_post_earnings_calc.py` lines 320-329 (snapshots path), lines 407-416 (option_summary path), and `ei_backfill_metrics.py` lines 128-138
- `main.py` has 12 `coffee_break()` calls with `after_step=` arguments referencing STEP_SEQUENCE names — several must be updated
- The FM news enrichment pattern at `fm_main.py:1289-1313` is the model for the EI news enrichment sub-step
- All `run_*()` in `main_runners.py` return structured dicts (`{'success': bool, ...}`) not bools
- `clean_database_row()` from `tools/decimal_formatter.py` must be applied before every INSERT/UPDATE

## Tasks

- [x] 1.0 Foundation: IV Crush Severity Scale
  - [x] 1.1 Update IV crush severity scale in `ei_post_earnings_calc.py` — **two locations** (lines 320-329 and lines 407-416). Replace 4-tier scale (`severe >= 40`, `moderate >= 25`, `mild >= 10`, `minimal < 10`) with 5-tier scale: `severe` (> 65%), `high` (50-65%), `normal` (40-50%), `mild` (25-40%), `minimal` (< 25%). Both code paths must produce identical thresholds.
  - [x] 1.2 Update matching IV crush classification in `ei_backfill_metrics.py` (lines 128-138) to use the same 5-tier scale. This is a consistency measure — the backfill script is not run in production daily, but should match if ever re-run.

- [x] 2.0 Core: `earnings_watchlist` Table & Core Population from Primary Sources
  - [x] 2.1 Create `strategies/earnings_intel/ei_watchlist.py` with module docstring, imports (`sqlite3`, `logging`, `tools/decimal_formatter`, `tools/timezone_utils`), and module-level `beautiful_log` setup per `docs/CONSOLE_DEVELOPER_GUIDE.md`.
  - [x] 2.2 Implement `_create_table(cursor)` — CREATE TABLE IF NOT EXISTS `earnings_watchlist` with all 24 columns from PRD Req 11 (PK = `symbol`). Include 2 indexes: `idx_ew_earnings_date` on `earnings_date`, `idx_ew_status` on `status`. All in a single function that can be called at module initialization.
  - [x] 2.3 Implement `_cleanup_expired(cursor, today)` — DELETE rows where `earnings_date` is more than 3 calendar days before today (i.e., T+4 or beyond). Simple date arithmetic: `DELETE FROM earnings_watchlist WHERE julianday(today) - julianday(earnings_date) >= 4`. Log how many rows were removed. Return `removed_count`.
  - [x] 2.4 Implement `_get_qualifying_symbols(cursor)` — Query `earnings_upcoming` for symbols matching entry criteria: `earnings_play_signal` IN ('WATCH', 'BUY', 'STRONG BUY'), `days_to_earnings` <= 14 (calendar days), total OI >= 4000. Also include symbols already on the watchlist that are now post-earnings (T+1/T+2/T+3) — they stay until T+4 regardless of signal changes.
  - [x] 2.5 Implement `_compute_status_and_days(earnings_date, today)` — Returns `(status, days_to_earnings)` tuple. Simple calendar day arithmetic: `days = (earnings_date - today).days`. Status values: `UPCOMING` (days > 0), `TODAY` (days == 0), `T+1`/`T+2`/`T+3` (days is -1/-2/-3). No external dependencies — just date subtraction.
  - [x] 2.6 Implement `_pull_option_summary_data(cursor, symbols)` — Batch query `option_symbol_summary` for `close_price` (→ `current_price`), `symbol_iv_percentile_30d` (→ `iv_percentile_30d`), `oi_balance_text`. Match by symbol + most recent `trade_date`. Return dict keyed by symbol.
  - [x] 2.7 Implement the main `populate_watchlist(db_path)` function. Orchestrates: open connection → create table → cleanup expired → get qualifying symbols → compute status/days → pull option_summary data → build row dicts with NULLs for post-earnings and cross-strategy columns (actual_move_pct, move_direction, iv_collapse_pct, iv_crush_severity, alert_count_5d, news columns) → apply `clean_database_row()` → execute `INSERT ... ON CONFLICT(symbol) DO UPDATE SET ...`. The UPDATE SET clause must list ALL columns EXCEPT `symbol`, `first_appeared_date`, and `created_at` (these three are preserved from the original INSERT). Use `now_eastern()` for `created_at` and `last_updated` timestamps.
  - [x] 2.8 Build and return the structured result dict: `watchlist_count` (total rows after run), `watchlist_new` (rows where INSERT happened, not UPDATE), `watchlist_breakdown` (dict of signal → count), `watchlist_symbols` (list of full row dicts for console display), `removed_count` (from cleanup step). To detect new vs updated: compare symbols before and after, or use `changes()` on the cursor.
  - [x] 2.9 Wrap the entire `populate_watchlist()` in try/except. Fatal failures (e.g., earnings_upcoming query fails, DB connection error) queue via `queue_error()` with severity `ERROR`, context including `failed_step='watchlist_population'` and `error_message`. Individual symbol failures (e.g., missing IV percentile) log as WARNING and skip that field, not the whole symbol.

- [x] 3.0 Enrichment: Post-Earnings Data & Cross-Strategy Context
  - [x] 3.1 Implement `_enrich_post_earnings(cursor, watchlist_rows, today)` in `ei_watchlist.py`. For rows with status T+1 or T+2: query `historical_prices` for `close_price` on the trading day BEFORE `earnings_date` (pre-earnings close) and the most recent available `trade_date` (at 7 AM, this is yesterday's close). Calculate `actual_move_pct = (recent_close - pre_earnings_close) / pre_earnings_close * 100`. Set `move_direction` = 'UP' or 'DOWN'. For T+3 rows: pull `move_3day_pct` (→ `actual_move_pct`), `iv_collapse_pct`, and `iv_crush_severity` from `earnings_moves`. All fields NULL for UPCOMING/TODAY.
  - [x] 3.2 Implement `_enrich_flow_alerts(cursor, symbols)` in `ei_watchlist.py`. Query `flow_symbol_summary` for `alert_count_5d` matched by symbol. Return dict keyed by symbol. NULL if no row exists (symbol has no recent flow alerts).
  - [x] 3.3 Implement `_apply_enrichments(cursor, enrichment_data)` — UPDATE `earnings_watchlist` SET the enrichment columns for each symbol. This is a separate UPDATE pass after the initial INSERT/upsert from task 2.0. Columns updated: `actual_move_pct`, `move_direction`, `iv_collapse_pct`, `iv_crush_severity`, `alert_count_5d`. Apply `clean_database_row()` before UPDATE. Individual symbol UPDATE failures log as WARNING and continue.
  - [x] 3.4 Integrate enrichment into `populate_watchlist()`: after the INSERT/upsert pass (task 2.7), call `_enrich_post_earnings()` and `_enrich_flow_alerts()`, then `_apply_enrichments()`. The enrichment step is wrapped in its own try/except — if enrichment fails entirely, the base watchlist data from task 2.0 is still committed.

- [x] 4.0 Unified EI Pipeline in `ei_main.py`
  - [x] 4.1 Restructure `run_daily_pipeline()` in `ei_main.py` (currently lines 277-476) to run 6 sub-steps, each in its own try/except. Preserve the existing sub-steps 1-3 (snapshots, post-earnings calc, expected moves) and add sub-steps 4-6 (watchlist, news enrichment, arbitrage). Each sub-step records `{'success': bool, 'duration_seconds': float, ...}` in a `sub_tasks` dict.
  - [x] 4.2 Add sub-step 4: Call `populate_watchlist()` from `ei_watchlist.py`. Feed its return dict into the pipeline's `sub_tasks['watchlist']` and top-level summary fields (`watchlist_count`, `watchlist_new`, `watchlist_breakdown`, `watchlist_symbols`).
  - [x] 4.3 Add sub-step 5: News enrichment. Model after the FM pattern at `fm_main.py:1289-1313`. Logic: (a) Get symbols from watchlist where `first_appeared_date` = today (new entries) OR `days_to_earnings` = 1 (T-1 refresh). (b) For each symbol, check `news_symbol_sentiment` table: `SELECT COUNT(*) FROM news_symbol_sentiment WHERE symbol=? AND DATE(fetched_at) = DATE('now')` — skip if already fetched today. (c) Call `enrich_watchlist_batch()` or individual `fetch_symbol_news()` from `tools/news_sentiment.py`. (d) UPDATE `earnings_watchlist` SET `news_sentiment_label`, `news_sentiment_score`, `news_article_count` for enriched symbols. (e) On budget exhaustion: log clearly (`"NEWS API BUDGET EXHAUSTED (N/25 calls used). Skipped news for: SYM1, SYM2"`), do NOT queue to autofix — this is a budget limit, not an error. (f) Record `news_enriched` count in sub_tasks dict.
  - [x] 4.4 Add sub-step 6: Move the existing arbitrage scanner call (`scan_morning_opportunities()` from `ei_arbitrage_scanner.py`) here as the final sub-step. Currently this is `run_morning_scan()` lines 478-622 — extract the core call and integrate it as sub-step 6 with its own try/except and timing.
  - [x] 4.5 Implement sub-step failure cascading per PRD Req 28: Sub-steps 1-3 are independent of each other. Sub-step 4 (watchlist) reads from `earnings_upcoming` written by sub-step 3, but can use existing data if sub-step 3 fails. Sub-step 5 (news) depends on sub-step 4 producing watchlist rows — if sub-step 4 failed entirely, log "no watchlist entries to enrich" and return cleanly. Sub-step 6 (arbitrage) is fully independent. Top-level `success` is True if the pipeline completed (even with sub-step failures), False only if the pipeline itself crashes.
  - [x] 4.6 Build the unified return dict per PRD Req 10: `success`, `duration_seconds`, `errors` (count of failed sub-steps), `sub_tasks` dict with 6 entries, plus top-level summary fields: `snapshots_created`, `moves_calculated`, `signals_updated`, `watchlist_count`, `watchlist_new`, `watchlist_breakdown`, `watchlist_symbols`, `arbitrage_opportunities`, `alerts_triggered`, `alert_details`.
  - [x] 4.7 Update or remove old `run_morning_scan()` function. Its core logic (arbitrage scan) is now sub-step 6 inside `run_daily_pipeline()`. If standalone CLI use (`--morning-scan`) should still work, keep the function but have it internally call `run_daily_pipeline()` or just the arbitrage sub-step. Otherwise, remove it and update CLI handling in `ei_main.py`'s `__main__` block.

- [x] 5.0 Orchestrator: Step Renumbering in `main.py`
  - [x] 5.1 Update `STEP_SEQUENCE` list (lines 154-174): Remove `'Earnings Arbitrage Scanner'` and `'Earnings Pipeline'`. Add `'Earnings Intelligence'` after `'Morning Option Pipeline'`. Rename `'Airline Play Tracking'` position from index 9 to index 8 (it moves from 3.4 to 3.3). The final list should have 16 entries (was 17 — removed 2, added 1).
  - [x] 5.2 Replace Step 1.2 block (lines 426-432): Change from calling `run_earnings_morning_scan()` to calling `run_earnings_intelligence()`. Update the step label from `"Step 1.2: Earnings Arbitrage Morning Scan"` to `"Step 1.2: Earnings Intelligence"`. Update result/duration keys from `'1.2 Arbitrage Scanner'` to `'1.2 Earnings Intelligence'`.
  - [x] 5.3 Update `coffee_break` call at line 435: Change `after_step="Earnings Arbitrage Scanner"` to `after_step="Earnings Intelligence"`.
  - [x] 5.4 Remove Step 3.3 block (lines 488-494): Delete the entire Earnings Alert Pipeline step (beautiful_log, timing, results assignment, _save_orchestrator_result).
  - [x] 5.5 Remove the `coffee_break` at line 497 (`after_step="Earnings Pipeline"`) since Step 3.3 no longer exists. The coffee_break before what was 3.4 (now 3.3) should use `after_step="Query Database Sync (Evening)"` — which is already the coffee_break at line 489.
  - [x] 5.6 Renumber evening Step 3.4 → 3.3: Update label `"Step 3.4: Airline Play Tracking Phase"` → `"Step 3.3: Airline Play Tracking Phase"`. Update keys `'3.4 Airline Play'` → `'3.3 Airline Play'` in results and step_durations.
  - [x] 5.7 Renumber evening Step 3.5 → 3.4: Update label `"Step 3.5: Query Database Sync (Final)"` → `"Step 3.4: Query Database Sync (Final)"`. Update keys `'3.5 Query Sync (Final)'` → `'3.4 Query Sync (Final)'` in results and step_durations.
  - [x] 5.8 Update `coffee_break` at line 515: Change `after_step="Query Database Sync (Final)"` — this one stays the same name, but verify it still matches the STEP_SEQUENCE entry.
  - [x] 5.9 Update `--earnings-intel` CLI handler (line 717-719): Change `orchestrator.run_earnings_pipeline()` to `orchestrator.run_earnings_intelligence()`.

- [x] 6.0 Orchestrator: `run_earnings_intelligence()` & Console Display in `main_runners.py`
  - [x] 6.1 Create `run_earnings_intelligence()` in `main_runners.py`. Follow the pattern of existing `run_earnings_pipeline()` (lines 1006-1098): create mission box describing what EI is about to do, call `run_daily_pipeline()` from `ei_main`, build completion box from return dict, handle failure with `queue_error()`. This replaces both `run_earnings_pipeline()` and `run_earnings_morning_scan()`.
  - [x] 6.2 Build the completion box contents from the return dict: "Snapshots collected: N", "Moves calculated: N", "Signals updated: N", "Watchlist: N symbols (X new) — STRONG BUY: A, BUY: B, WATCH: C", "Arbitrage opportunities: N", "Earnings alerts: N" with signal breakdown and top symbols (same alert format as current `run_earnings_pipeline()`).
  - [x] 6.3 Implement the earnings watchlist ASCII table renderer. Consumes `watchlist_symbols` list from the return dict. Sorted by `days_to_earnings` ascending. Column display priority: `symbol`, `status`, `current_price`, `days_to_earnings` (header: "Days"), `earnings_time`, `earnings_play_signal` (header: "Signal"), `iv_percentile_30d` (header: "IV%ile"), `relative_underpricing_pct` (header: "Underprc%"), `expected_move_pct` (header: "ExpMv%"), `oi_balance_text` (header: "OI Bal"), `news_sentiment_label` (header: "Sentiment"). Post-earnings columns for T+N rows: `actual_move_pct` (header: "ActMv%"), `move_direction` (header: "Dir"), `iv_crush_severity` (header: "Crush"). Use box-drawing characters, fit within 120-char terminal width. Use `tools/log_utils.py` output functions so output reaches both console and `.log` file.
  - [x] 6.4 Handle empty watchlist: if `watchlist_count` == 0, display a single line "No earnings watchlist entries" instead of an empty table. Skip the table renderer entirely.
  - [x] 6.5 On unhandled exception in the pipeline call: create a failure result dict (`{'success': False, 'duration_seconds': ..., 'errors': 1}`), queue via `queue_error()` matching the existing pattern in `run_earnings_pipeline()`, display failure box.
  - [x] 6.6 Remove old `run_earnings_pipeline()` (lines 1006-1098) and `run_earnings_morning_scan()` (lines 1100-1140). Update the import at the top of `main_runners.py` from `from strategies.earnings_intel.ei_main import run_daily_pipeline, run_morning_scan, run_weekly_refresh` — remove `run_morning_scan` if no longer needed, keep `run_daily_pipeline` and `run_weekly_refresh`.

- [x] 7.0 Cross-Strategy: Flow Watchlist Enrichment
  - [x] 7.1 Add `ALTER TABLE flow_watchlist_daily ADD COLUMN earnings_play_signal TEXT` and `ALTER TABLE flow_watchlist_daily ADD COLUMN relative_underpricing_pct REAL` in `fm_watchlist.py`. Use try/except to catch "duplicate column name" (standard idempotent pattern). Run at module initialization or inside `update_daily_watchlist()` before first use.
  - [x] 7.2 Update `_fetch_earnings_lookup()` (line 33) to also SELECT `earnings_play_signal` and `relative_underpricing_pct` from `earnings_upcoming` alongside the existing `earnings_date`, `earnings_time` fields. Include both in the returned lookup dict.
  - [x] 7.3 Update `update_daily_watchlist()` (line 83) INSERT statement to include `earnings_play_signal` and `relative_underpricing_pct` from the earnings lookup dict. These are populated at INSERT time only — no UPDATE on subsequent scans (INSERT-only enrichment per PRD Req 32).
  - [x] 7.4 Verify that existing rows (created before this change) have NULL for the new columns, which is correct behavior — they were created without earnings signal context.

- [x] 8.0 Performance Writer Update & Schema Migration
  - [x] 8.1 Update `ei_mapping` dict in `performance_writer.py` (line 635): Remove `'1.2 Arbitrage Scanner': 'morning_scan'`. Change `'3.3 Earnings Pipeline': 'daily_pipeline'` to `'1.2 Earnings Intelligence': 'daily_pipeline'`. Keep `'5.2 Earnings Refresh': 'weekly_refresh'` unchanged.
  - [x] 8.2 Update any other step key references in `performance_writer.py` for the evening renumbering: `'3.4 Airline Play'` → `'3.3 Airline Play'`, `'3.5 Query Sync (Final)'` → `'3.4 Query Sync (Final)'`. Search all mapping dicts in the file (not just `ei_mapping`).
  - [x] 8.3 Add 5 new columns to the `ei_pipeline_performance` CREATE TABLE DDL (lines 101-137): `watchlist_count INTEGER`, `watchlist_new INTEGER`, `news_enriched INTEGER`, `watchlist_seconds REAL`, `news_seconds REAL`. Place them after the existing metric columns.
  - [x] 8.4 Add `ALTER TABLE ei_pipeline_performance ADD COLUMN ...` statements with try/except for "duplicate column name" for all 5 new columns. Run at writer initialization time (same idempotent pattern as Req 23). This migrates existing `data/performance.db` databases.
  - [x] 8.5 Update `_write_ei_performance()` (lines 630-709) to extract the new fields from the result dict for `daily_pipeline` run type: `watchlist_count` from top-level, `watchlist_new` from top-level, `news_enriched` from `sub_tasks.news_enrichment`, `watchlist_seconds` from `sub_tasks.watchlist.duration_seconds`, `news_seconds` from `sub_tasks.news_enrichment.duration_seconds`. Add these to the INSERT statement's column list and values.
  - [x] 8.6 Verify step key consistency: After all renumbering is complete, confirm that every key in `main.py`'s `step_durations` and `results` dicts has a matching entry in the performance_writer's mapping dicts. This is the explicit lesson from PRD 0007's post-launch key mismatch fix. Create a quick checklist comparing all keys.
